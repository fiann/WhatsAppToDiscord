name: ci-tests

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  tests:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node: [20, 22]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm

      - name: Install Node dependencies
        run: npm ci

      - name: Run Node unit tests
        run: npm test

      - name: Bundle with esbuild (release smoke)
        if: matrix.os == 'ubuntu-latest' && matrix.node == 22
        run: |
          npx esbuild src/runner.js \
            --bundle --platform=node --format=cjs \
            --external:sharp --external:qrcode-terminal --external:jimp --external:link-preview-js \
            --target=node22 --outfile=out.js

      - uses: actions/setup-python@v5
        if: matrix.os == 'ubuntu-latest' && matrix.node == 20
        with:
          python-version: '3.11'
          cache: pip

      - name: Install Python dependencies
        if: matrix.os == 'ubuntu-latest' && matrix.node == 20
        run: pip install -r requirements.txt

      - name: Run Python smoke boot test
        if: matrix.os == 'ubuntu-latest' && matrix.node == 20
        run: pytest -q tests/test_boot.py
